---
mode: ContinuousDeployment

branches:
  latest:
    regex: ^latest$
    tag: 'release'
    increment: Minor
    source-branches: []

  UAT-52:
    regex: UAT-52
    tag: 'release'
    increment: Minor
    source-branches: []

assembly-versioning-scheme: MajorMinorPatch
