---
mode: ContinuousDeployment

branches:
  latest:
    regex: ^latest$
    tag: 'release'
    increment: Minor

  UAT-52:
    regex: UAT-52
    tag: 'release'
    increment: Minor

assembly-versioning-scheme: MajorMinorPatch
