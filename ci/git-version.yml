---
mode: ContinuousDeployment

branches:
  latest:
    regex: ^latest$
    tag: "release"
    increment: Minor
    source-branches:
      - __BRANCH_NAME__

assembly-versioning-scheme: MajorMinorPatch
